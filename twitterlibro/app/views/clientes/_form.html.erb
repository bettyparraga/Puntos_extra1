<%= form_for(@cliente) do |f| %>
  <% if @cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% @cliente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Correo electrónico" %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label "Nombre y Apellidos" %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label "Cuenta Twitter" %><br />
    <%= f.text_field :dirtwitter %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
